import * as React from 'react';
import { OptionsPageContent } from './index';

export const OptionsPage = () => {
  // @ts-ignore - Ignore TypeScript errors for now to get the extension working
  return <OptionsPageContent />;
}; 